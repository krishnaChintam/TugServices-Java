package com.ship.services.repo;

import com.ship.services.model.TugServiceActivity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TugServiceActivityRepository extends JpaRepository<TugServiceActivity, Long> {
}
